##########################################################################################################################################

#(주의) ->  순차적으로 코드를 실행하는 것을 권함!

#에러 발생 시 github Finance/R-Finance-Programming 경로에 issue를 남기면 확인

##########################################################################################################################################

### 벡터화(vectorization)

# 벡터화 =  간단한 코딩 + 빠른 속도.

# 어떤 함수 f(x)가 있는데 그 입력변수인 x는 숫자(scalar)이다.
# 만약에 x의 여러가지 값에 대해서 함수를 적용해야 한다면 일반적으로는 for와 같은 반복문을 사용.
# 벡터 개념을 이용하면 벡터를 입력하고 벡터를 출력함으로써 보다 간단한 코드 작성.

# 벡터화는 벡터 원소 각각에 대해 연산을 하는 것이 아니라, 벡터 전체에 대해 한꺼번에 연산을 하는 것을 의미
# 원소별로 연산을 하는 것보다 벡터화 시키면 편하고 빠름.

a<-1:10
b<-21:30
c<-0

for(i in 1:10){
  c[i]<-a[i]+b[i]
}

c

# 벡터화를 이용하기 전 직관적인 코드 구현.

############################################################결과값(print)#################################################################

# > a<-1:10
# > b<-21:30
# > c<-0

# > 

#  > for(i in 1:10){
#    +   c[i]<-a[i]+b[i]
#    + }

# > 

#  > c
# [1] 22 24 26 28 30 32 34 36 38 40

##########################################################################################################################################

a<-1:10
b<-21:30
c<-a+b

c

# 벡터화를 이용한 케이스.

############################################################결과값(print)#################################################################

# > a<-1:10
# > b<-21:30
# > c<-a+b

# > 

#  > c
# [1] 22 24 26 28 30 32 34 36 38 40

##########################################################################################################################################

v<-1:1000
for(i in 1:1000){
  v[i]<-v[i]+1
}

v<-1:1000
v<-v+1

# 벡터연산이 중요한 이유는 순환문 등을 사용해 값을 하나씩 처리해가는 대신 벡터나 리스트를 한번에 처리하는 것이 더 효율적이고 편리함.

############################################################결과값(print)#################################################################

# > v<-1:1000
# > for(i in 1:1000){
#  +   v[i]<-v[i]+1
#  + }

# > 

#  > v<-1:1000
# > v<-v+1

##########################################################################################################################################

weight.x<-c(10,20,30)
weight.sq<-weight.x^2

# 스칼라 인수를 받는 함수에서 특정 인수(input variable)가 벡터이면 그 결과를 벡터로 반환.
# 기본적인 사칙연산 이외에 sqrt, log, exp, sin 등의 함수에도 벡터화 적용.
# 제곱함수로 벡터화 연산 수행.(벡터를 입력받아 한꺼번에 벡터로 반환.)

############################################################결과값(print)#################################################################

# > weight.x<-c(10,20,30)
# > weight.sq<-weight.x^2

##########################################################################################################################################
