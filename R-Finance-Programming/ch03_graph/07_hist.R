##########################################################################################################################################

#(주의) ->  순차적으로 코드를 실행하는 것을 권함!

#에러 발생 시 github Finance/R-Finance-Programming 경로에 issue를 남기면 확인

##########################################################################################################################################



### hist(히스토그램)

## 히스토그램을 그리는 함수

##########################################################################################################################################

### hist()

x<-c(3,0.5,0.5,0.5,3,2,0.5,0.5,3,3,1)
tmp<-hist(x)


# 2.0 항목이 1건, 히스토그램은 1.5 -2.0 구간에 1건이 표시됨.
# 1.5 -2.0 구간은 1.5를 포함하지 않고, 2.0은 포함함을 알 수 있음.
# 왼쪽불포함/오른쪽포함(왼쪽 개구간/ 오른쪽 폐구간.)

# 시작구간은 위 원칙이 적용되지 않고 왼쪽도 폐구간(포함).
# 시작구간의 시작값과 종료구간의 종료값을 통해 알 수 있는 사실은 아래와 같음.
# hist 함수는 기본적으로 시작구간의 시작값은 최소값으로, 종료구간의 종료값은 최대값으로 설정.



graphics.off()                    # 그래프 지워주는 함수.

############################################################결과값(print)#################################################################

# 그래프 찍어보기.


##########################################################################################################################################
